name: pcalc_clang_constexpr
description: Typed C and C++ constant-expression evaluation using Clang.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  code_assets: 1.0.0
  ffi: ^2.1.4
  hooks: 1.0.3

dev_dependencies:
  test: ^1.26.3
